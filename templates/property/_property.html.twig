<div class="card mb-4">	
	{% if property.fileName %}
		<img src="{{vich_uploader_asset(property,'imageFile') | imagine_filter('thumb')}}"  alt="card-img-top"  style="width:100%; height:auto%">
		{# src="{{vich_uploader_asset(property,'imageFile')}}" #}
		{# src="/images/properties/{{ property.fileName }}" #}
	{% else %}
		<img src="{{'/images/properties/iguane2.jpg' | imagine_filter('thumb')}}"  alt="card-img-top"  style="width:100%; height:auto%">

	{% endif %}
	<div class="card-body">	
		<h5 class="card-title">
		<a href="{{ path ('property.show', {slug: property.slug, id: property.id}) }}" >{{ property.title}}</a>
		</h5>
		{# <p class="card-text">{{ property.description | nl2br}}	</p> #}
		<p class="card-text">{{ property.surface }} m2 <br>{{ property.city }} ({{ property.postalCode }})</p>
		<div class="text-primary" style="font-size:2em; font-weight:bold">	{{ property.formattedPrice }} €</div>
	</div>
</div>
